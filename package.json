{
  "name": "jokerag",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:lib && npm run build:dashboard",
    "build:lib": "tsup src/index.ts --format esm --dts",
    "build:dashboard": "tsup src/dashboard-browser.ts --format iife --global-name JokerAgDashboard",
    "check": "npm run fmt:check && npm run lint && npm run typecheck && npm run test:coverage",
    "fmt": "prettier --write \"src/**/*.{ts,tsx}\" \"tests/**/*.{ts,tsx}\" \"*.md\"",
    "fmt:check": "prettier --check \"src/**/*.{ts,tsx}\" \"tests/**/*.{ts,tsx}\" \"*.md\"",
    "lint": "eslint src tests --ext .ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@typescript-eslint/eslint-plugin": "^8.25.0",
    "@typescript-eslint/parser": "^8.25.0",
    "@vitest/coverage-v8": "^3.0.8",
    "eslint": "^9.21.0",
    "prettier": "^3.5.1",
    "tsup": "^8.4.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
